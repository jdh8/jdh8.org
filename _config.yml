timezone: Asia/Taipei

plugins:
  - jekyll-paginate
  - jekyll-archives

jekyll-archives:
  enabled: [tags, categories]
  layouts:
    tag: tag
    category: category

title: Taipei Forcing Club
description: Computer science and contract bridge
paginate: 10
paginate_path: /page/:num/
permalink: /:title/

defaults:
  - scope:
        path: ""
    values:
        author: jdh8
